package com.formula1.simulation;

import com.formula1.model.Circuit;
import com.formula1.model.Driver;
import com.formula1.model.SimulationConfig;
import com.formula1.model.Vehicle;
import com.formula1.model.Weather;

/**
 * Calcula el tiempo de vuelta de un piloto (HU-16) combinando piloto,
 * vehículo, circuito, clima y configuración de la simulación.
 */
public class LapCalculator {

    public double calculateLapTime(Driver driver, Vehicle vehicle, Circuit circuit, Weather weather, SimulationConfig config) {
        throw new UnsupportedOperationException(
                "TODO: implementar fórmula de tiempo de vuelta (piloto + vehículo + circuito + clima + configuración)");
    }
}
